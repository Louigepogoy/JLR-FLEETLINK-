PORT=5000
NODE_ENV=development

# Neon PostgreSQL (recommended)
# Copy from Neon Dashboard → Connection string → pooled
# Example:
# DATABASE_URL=postgresql://user:password@ep-xxxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_E3hQ5CFfIKZy@ep-icy-frog-apjcsmjd-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
DATABASE_SSL=true

# Local PostgreSQL (alternative)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/jlr_fleetlink
# DATABASE_SSL=false

JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

FRONTEND_URL=http://localhost:3000
API_BASE_URL=http://localhost:5000

# Xendit payment gateway (real GCash/card checkout for bookings and subscriptions).
# Sign up free at https://dashboard.xendit.co, grab the Test Mode Secret Key from
# Settings -> API Keys, and the Webhook Verification Token from Settings -> Webhooks.
XENDIT_SECRET_KEY=your-xendit-secret-key
XENDIT_WEBHOOK_TOKEN=your-xendit-webhook-verification-token

# Gmail SMTP for login OTP emails
# Use a Gmail account with 2-Step Verification enabled, then generate an
# "App Password" (Google Account -> Security -> App passwords) — do NOT use your normal Gmail password.
EMAIL_USER=your-gmail-address@gmail.com
EMAIL_APP_PASSWORD=your-16-character-app-password

# Google Gemini API key for AI-assisted license/vehicle verification in the admin dashboard.
# Get a free key (no credit card needed) at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
